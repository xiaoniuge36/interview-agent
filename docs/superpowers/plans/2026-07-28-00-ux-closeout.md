# UX 收口 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox syntax for tracking.

**Goal:** 收口当前 Home Welcome、推荐栏和前后台 Agent 入口改动，使主分支回到可验证、可演示状态。

**Architecture:** 不引入新能力或共享契约，只完成已存在的 UX 改动和对应测试。该提交不得混入 Prisma、contracts、Runtime 或根配置变更。

**Tech Stack:** Next.js 15、React 18、Vitest、TypeScript、Prettier、ESLint。

---

## 文件边界

- Modify: apps/user-portal/src/components/home/question-hub/HomeWelcome.tsx
- Modify: apps/user-portal/src/components/home/question-hub/AgentRecommendationRail.tsx
- Modify: apps/user-portal/src/components/home/question-hub/QuestionHubPage.tsx
- Modify: apps/user-portal/src/components/user-agent/UserAgentFloatButton.tsx
- Modify: apps/admin-console/src/components/admin-agent/AdminAgentDrawerContent.tsx
- Modify: apps/admin-console/src/components/admin-agent/AdminAgentFloatButton.tsx
- Test: apps/user-portal/src/components/home/question-hub/HomeWelcome.test.tsx
- Test: apps/user-portal/src/components/home/question-hub/home-welcome-model.test.ts
- Test: apps/user-portal/src/components/home/question-hub/AgentRecommendationRail.test.tsx
- Test: apps/admin-console/src/components/admin-agent/AdminAgentFloatButton.test.tsx

### Task 1: 固定 Home Welcome 的纯显示模型

- [ ] Step 1: 先扩展模型测试，覆盖首次进入、继续训练、查看复盘。

  expect(homeWelcomeModel({ displayName: "候选人", continuation: null }).title).toContain("候选人");
  expect(homeWelcomeModel({ displayName: null, continuation: activePractice }).actionLabel).toBe("继续训练");
  expect(homeWelcomeModel({ displayName: null, continuation: completedPractice }).actionLabel).toBe("查看复盘");

- [ ] Step 2: 实现只依赖 displayName 和 TrainingContinuation 的纯函数。

  export function homeWelcomeModel(input: HomeWelcomeInput): HomeWelcomeModel {
  return input.continuation ? continuationWelcome(input) : freshWelcome(input.displayName);
  }

- [ ] Step 3: 运行用户端定向测试。

Run: pnpm --filter @interview-agent/user-portal test -- HomeWelcome.test.tsx home-welcome-model.test.ts AgentRecommendationRail.test.tsx

Expected: 所有测试通过，无快照更新。

### Task 2: 收口前后台 Agent 入口

- [ ] Step 1: 为浮动入口保留稳定的可访问名称和展开状态。

<button aria-label="打开 AI 刷题教练" aria-expanded={open} onClick={onOpen}>
  <SparklesIcon aria-hidden="true" />
</button>

- [ ] Step 2: 保持历史列表使用现有 conversations/runs，不从展示组件增加请求。

<UserAgentRunHistory runs={props.runs} onSelect={props.onSelectRun} />

- [ ] Step 3: 运行两端定向测试。

Run: pnpm --filter @interview-agent/user-portal test -- UserAgentFloatButton.test.tsx && pnpm --filter @interview-agent/admin-console test -- AdminAgentDrawer.test.tsx AdminAgentFloatButton.test.tsx

Expected: 按钮名称、打开行为和历史入口全部通过。

### Task 3: 执行收口门禁并提交

- [ ] Step 1: 检查工作区只包含本计划的 UX 改动。

Run: git status --short

Expected: 没有 schema、contract、Runtime、依赖或 CI 改动。

- [ ] Step 2: 运行格式、lint、typecheck 和两个前端 build。

Run: pnpm format:check && pnpm --filter @interview-agent/user-portal lint && pnpm --filter @interview-agent/admin-console lint && pnpm --filter @interview-agent/user-portal typecheck && pnpm --filter @interview-agent/admin-console typecheck && pnpm --filter @interview-agent/user-portal build && pnpm --filter @interview-agent/admin-console build

Expected: 全部通过。

- [ ] Step 3: 提交独立 UX 收口。

  git add apps/user-portal apps/admin-console docs/superpowers/plans
  git commit -m "feat(ui): 收口首页欢迎与助手入口"
